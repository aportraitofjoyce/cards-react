const initialState = {}

export const passwordRecoveryReducer = (state = initialState, action: any): typeof initialState => {
    switch (action.type) {
        default:
            return state
    }
}

//THUNKS

